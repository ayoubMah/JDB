<!DOCTYPE html>
<html>
<head>
    <title>Blog List</title>
</head>
<body>
<h1>Blog List</h1>
<ul>
    {% for post in posts %}
        <li><a href="{{ path('blog_show', {'slug': post.slug}) }}">{{ post.title }}</a></li>
    {% endfor %}
</ul>
</body>
</html>
